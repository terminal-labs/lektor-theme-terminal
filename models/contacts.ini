[model]
name = Contact Page
inherits = page
label = {{ this.title }}

[fields.contact_methods]
label = contact_methods
type = flow
flow_blocks = contact_method

[fields.contacts]
label = contacts
type = flow
flow_blocks = contact
